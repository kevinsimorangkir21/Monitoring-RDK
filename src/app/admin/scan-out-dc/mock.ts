/**
 * Scan Out DC — Mock data kosong.
 * Data diisi melalui CRUD (local state).
 */

import type { ScanOutEntry } from "./types";

export const scanOutData: ScanOutEntry[] = [];

export const VENDOR_OPTIONS: string[] = ["Balrich", "Majur", "GTU"];
